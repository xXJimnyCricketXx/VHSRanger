.env
/node_modules
/public/uploads/avatars/*
/mongo_data
/backups
docker-compose.override.yml
.vscode/
*.archive

# Files provided by the user for reference during development (screenshots,
# exports, sample assets, etc.) - not part of the app itself.
/inbox/
