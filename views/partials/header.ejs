<!DOCTYPE html>
<% 
  const isDark = (typeof locals.isDark !== 'undefined') ? locals.isDark : true;
  const currentLng = (typeof locals.currentLng !== 'undefined') ? locals.currentLng : 'fr';
  const THEMES = (typeof locals.allThemes !== 'undefined') ? locals.allThemes : {};

  let activePresetKey = 'ranger';
  if (typeof settings !== 'undefined' && settings && settings.theme && settings.theme.preset) {
      activePresetKey = settings.theme.preset;
  }

  const safeFallback = {
      light: { bg: '#e2e8f0', card: '#ffffff', navbar: '#1e293b', navText: '#ffffff', text: '#0f172a', subtext: '#475569', highlight: '#10b981' },
      dark:  { bg: '#171717', card: '#262626', navbar: '#0a0a0a', navText: '#ffffff', text: '#ffffff', subtext: '#a3a3a3', highlight: '#10b981' }
  };

  const activeTheme = (THEMES[activePresetKey]) ? THEMES[activePresetKey] : (THEMES['ranger'] || safeFallback);
%>

<html lang="<%= currentLng %>" class="<%= isDark ? 'dark' : '' %> <%= activePresetKey === 'ranger' ? 'theme-ranger' : '' %>">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>VHSRanger!</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            theme: {
                bg: 'var(--bg-page)',
                card: 'var(--bg-card)',
                nav: 'var(--bg-nav)',
                text: 'var(--text-main)',
                subtext: 'var(--text-sub)',
                highlight: 'var(--highlight)'
            }
          }
        }
      }
    };
  </script>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.js"></script>
  <link rel="icon" type="image/png" href="<%= baseUrl %>/ressources/logo.png">

  <style>
    :root {
        --bg-page: <%= activeTheme.light.bg %>;
        --bg-card: <%= activeTheme.light.card %>;
        --bg-nav:  <%= activeTheme.light.navbar %>;
        --text-main: <%= activeTheme.light.text %>;
        --text-sub: <%= activeTheme.light.subtext %>;
        --highlight: <%= activeTheme.light.highlight %>;
        --highlight-text: <%= activeTheme.light.highlightText || '#fff' %>;
        --nav-text: <%= activeTheme.light.navText || activeTheme.light.subtext %>;
        --accent-text: <%= activeTheme.light.accentText || activeTheme.light.highlight %>;
        --bg-footer: <%= activeTheme.light.footer || activeTheme.light.navbar %>;
        --footer-text: <%= activeTheme.light.footerText || activeTheme.light.navText || activeTheme.light.subtext %>;

        --shadow-color: 0 0 0 rgba(0,0,0,0.05);
        --border-color: rgba(0,0,0,0.1);
    }

    html.dark {
        --bg-page: <%= activeTheme.dark.bg %>;
        --bg-card: <%= activeTheme.dark.card %>;
        --bg-nav:  <%= activeTheme.dark.navbar %>;
        --text-main: <%= activeTheme.dark.text %>;
        --text-sub: <%= activeTheme.dark.subtext %>;
        --highlight: <%= activeTheme.dark.highlight %>;
        --highlight-text: <%= activeTheme.dark.highlightText || '#fff' %>;
        --nav-text: <%= activeTheme.dark.navText || activeTheme.dark.subtext %>;
        --accent-text: <%= activeTheme.dark.accentText || activeTheme.dark.highlight %>;
        --bg-footer: <%= activeTheme.dark.footer || activeTheme.dark.navbar %>;
        --footer-text: <%= activeTheme.dark.footerText || activeTheme.dark.navText || activeTheme.dark.subtext %>;

        --shadow-color: 0 0 20px <%= activeTheme.dark.highlight %>20;
        --border-color: rgba(255,255,255,0.1);
    }

    html, body {
        margin: 0;
        padding: 0;
    }

    body {
        background-color: var(--bg-page) !important;
        color: var(--text-main) !important;
    }

    .nav-custom {
        background-color: var(--bg-nav) !important;
        border-bottom: 1px solid var(--border-color);
        color: var(--nav-text) !important;
    }

    .footer-custom {
        background-color: var(--bg-footer) !important;
        border-top: 1px solid var(--border-color);
        color: var(--footer-text) !important;
    }

    .nav-link {
        color: var(--nav-text) !important;
    }

    .text-primary-theme { color: var(--accent-text) !important; }
    .username-highlight { color: var(--highlight) !important; }
    .bg-primary-theme { background-color: var(--highlight) !important; color: var(--highlight-text) !important; }
    .border-primary-theme { border-color: var(--highlight) !important; }
    .hover\:text-primary-theme:hover { color: var(--accent-text) !important; }
    .hover\:bg-primary-theme:hover { background-color: var(--highlight) !important; color: var(--highlight-text) !important; }

    .card-theme {
        background-color: var(--bg-card) !important;
        border: 1px solid var(--border-color);
        color: var(--text-main) !important;
    }
    
    #page-loader {
      background-color: var(--bg-page);
      color: var(--text-main);
    }

    /* Custom dropdown arrow: native <select> arrows are drawn by the browser
       in a fixed-width control area that CSS padding cannot move, so we hide
       it (appearance:none) and place a real fa-chevron-down icon on top —
       same icon/classes as the custom dropdowns in the collection filters,
       for a visually identical result. */
    select.select-theme {
        appearance: none;
        -webkit-appearance: none;
        -moz-appearance: none;
        padding-right: 2.75rem !important;
    }
  </style>

  <link rel="manifest" href="<%= baseUrl %>/manifest.json">
  <meta name="theme-color" content="#3dc6d0">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="VHSRanger!">
  <link rel="apple-touch-icon" href="<%= baseUrl %>/ressources/icone-192.png">
</head>

<body class="font-sans transition-colors duration-300 min-h-screen flex flex-col">
    
  <div id="page-loader" class="fixed inset-0 z-[9999] flex flex-col items-center justify-center transition-opacity duration-500">
    <div class="relative">
        <i class="fa-solid fa-compact-disc fa-spin text-6xl text-primary-theme drop-shadow-md"></i>
    </div>
  </div>

  <nav class="nav-custom transition-colors duration-300">
      <div class="w-full flex flex-wrap items-center justify-between px-6 py-4">
      
      <a href="<%= baseUrl || '/' %>" class="flex items-center group">
        <img src="<%= baseUrl %>/ressources/<%= isDark ? 'logo/vhsranger-logo-dark.png' : 'logo/vhsranger-logo-light.png' %>" alt="VHSRanger!" class="w-48 h-auto">
      </a>

      <div class="flex items-center md:order-2 gap-4">
        
        <% if (locals.user) { %>
        <button type="button" class="flex items-center gap-2.5 text-sm outline-none" id="user-menu-button" data-dropdown-toggle="user-dropdown">
          <img class="w-9 h-9 rounded-full object-cover" src="<%= baseUrl + user.img %>" alt="user photo">
          <span class="hidden sm:flex flex-col items-start leading-tight">
            <span class="font-semibold text-[var(--nav-text)] max-w-[140px] truncate"><%= user.username %></span>
            <span class="text-[11px] opacity-50 text-[var(--nav-text)] max-w-[140px] truncate"><%= user.email %></span>
          </span>
          <i class="hidden sm:inline fa-solid fa-chevron-down text-[10px] opacity-50 text-[var(--nav-text)]"></i>
        </button>
  
        <div class="z-50 hidden my-4 text-base list-none card-theme rounded-lg shadow-xl min-w-[200px]" id="user-dropdown">          
          <div class="flex justify-center py-4 rounded-t-lg">
            <img src="<%= baseUrl + user.img %>" alt="user photo" class="w-20 h-20 rounded-full border-4 border-white dark:border-white/10 object-cover shadow-lg">
          </div>
       
          <div class="divide-y divide-black/5 dark:divide-white/10">
            <div class="px-4 pt-1 pb-3 text-center">
              <span class="block text-sm font-bold opacity-90 text-[var(--text-main)]"><%= user.username %></span>
              <span class="block text-xs opacity-60 truncate text-[var(--text-sub)]"><%= user.email %></span>
            </div>
            <ul class="py-2">
              <% if (user.isAdmin) { %>
              <li>
                <a href="<%= baseUrl %>/admin" class="block px-4 py-2 text-sm text-[var(--text-main)] hover:bg-black/5 dark:hover:bg-white/10 mx-2 rounded transition flex items-center gap-3">
                  <i class="fa-solid fa-crown w-5 text-center opacity-70"></i> <span><%= t('nav.admin') %></span>
                </a>
              </li>
              <li>
                <a href="<%= baseUrl %>/admin/personnalisation" class="block px-4 py-2 text-sm text-[var(--text-main)] hover:bg-black/5 dark:hover:bg-white/10 mx-2 rounded transition flex items-center gap-3">
                  <i class="fa-solid fa-wand-magic-sparkles w-5 text-center opacity-70"></i> <span><%= t('perso.title') %></span>
                </a>
              </li>
              <% } %>
              <li>
                <a href="<%= baseUrl %>/settings" class="block px-4 py-2 text-sm text-[var(--text-main)] hover:bg-black/5 dark:hover:bg-white/10 mx-2 rounded transition flex items-center gap-3">
                  <i class="fa-solid fa-user-gear w-5 text-center opacity-70"></i> <span><%= t('nav.settings') %></span>
                </a>
              </li>
              <li>
                <button id="theme-toggle" class="w-[calc(100%-1rem)] text-left block px-4 py-2 text-sm text-[var(--text-main)] hover:bg-black/5 dark:hover:bg-white/10 mx-2 rounded transition flex items-center gap-3">
                    <i id="theme-icon" class="fa-solid fa-moon w-5 text-center"></i> 
                    <span id="theme-text"><%= isDark ? t('nav.theme_light') : t('nav.theme_dark') %></span>
                </button>
              </li>

              <li class="pt-2 border-t border-black/5 dark:border-white/10 mt-2">
                <a href="<%= baseUrl %>/logout" class="block px-4 py-2 text-sm font-bold text-center bg-primary-theme hover:brightness-110 rounded-md mx-4 mb-2 transition-all shadow-sm">
                  <%= t('nav.logout') %>
                </a>
              </li>
            </ul>
          </div>
        </div>
        <% } else { %>
          <a href="<%= baseUrl %>/login" class="text-sm font-bold text-primary-theme hover:underline"><%= t('nav.login') %></a>
        <% } %>

        <button data-collapse-toggle="navbar-user" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm opacity-70 rounded-lg md:hidden hover:bg-black/5 dark:hover:bg-white/10 focus:outline-none text-[var(--nav-text)]">
          <i class="fa-solid fa-bars text-xl"></i>
        </button>
      </div>

      <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-user">
        <ul class="flex flex-col font-medium p-4 md:p-0 mt-4 rounded-lg md:flex-row md:space-x-8 md:mt-0 md:border-0 md:bg-transparent">
          <%
            const navLinks = [
                { label: t('nav.home'), url: '/' },
                { label: t('nav.collection'), url: '/collection' },
                { label: t('nav.wishlist'), url: '/wishlist' },
                { label: t('nav.stats'), url: '/stats', newTab: true },
            ];

            navLinks.forEach(item => { %>
                <li>
                  <a href="<%= baseUrl + item.url %>" <%= item.newTab ? 'target="_blank"' : '' %> class="nav-link block py-2 px-3 md:p-0">
                    <%= item.label %>
                  </a>
                </li>
            <% }); %>
        </ul>
      </div>
    </div>
  </nav>

  <script>
    document.body.classList.add('js-ready');
    const navTranslations = {
        themeDark: <%- JSON.stringify(t('nav.theme_dark')) %>,
        themeLight: <%- JSON.stringify(t('nav.theme_light')) %>
    };
    
    document.addEventListener('DOMContentLoaded', () => {
        const themeToggleBtn = document.getElementById('theme-toggle');
        const themeIcon = document.getElementById('theme-icon');
        const themeText = document.getElementById('theme-text');
        const htmlElement = document.documentElement;
  
        function updateButtonVisuals() {
            const isDark = htmlElement.classList.contains('dark');
            if(themeIcon && themeText) {
                if (isDark) {
                    themeIcon.className = 'fa-solid fa-sun w-5 text-center text-yellow-400';
                    themeText.textContent = navTranslations.themeLight;
                } else {
                    themeIcon.className = 'fa-solid fa-moon w-5 text-center text-indigo-500';
                    themeText.textContent = navTranslations.themeDark;
                }
            }
        }
        
        updateButtonVisuals();
  
        if(themeToggleBtn){
            themeToggleBtn.addEventListener('click', async () => {
                htmlElement.classList.toggle('dark');
                updateButtonVisuals();
                const newTheme = htmlElement.classList.contains('dark') ? 'dark' : 'light';
                try {
                    await fetch('<%= baseUrl %>/settings/update-theme', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ theme: newTheme })
                    });
                } catch (e) { console.error('Erreur thème', e); }
            });
        }
    });
    
    window.addEventListener('load', () => {
        const loader = document.getElementById('page-loader');
        if(loader) {
            loader.style.opacity = '0';
            setTimeout(() => { loader.remove(); }, 500);
        }
    });
  </script>

  <main class="p-6 flex-grow">
